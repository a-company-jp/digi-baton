import { SignOutButton } from "@clerk/nextjs";

export default function HomePage() {
  return (
    <main>
      <section>
        Hello World
      </section>
      <SignOutButton />
    </main>
  )
}